name: integration
private: true
description: interation test

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  functional_widget_annotation: ^0.5.0
  meta: ^1.1.7

dev_dependencies:
  flutter_hooks: any
  flutter_test:
    sdk: flutter
  pedantic: ^1.4.0
  build_runner: ^1.8.0
  functional_widget:
    path: ../

